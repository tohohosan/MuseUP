<div class="p-6 max-w-lg mx-auto bg-white rounded-xl shadow-md space-y-2">
  <h2 class="text-2xl font-bold text-center border-b-2 mb-6"><%= @notice.title %></h2>
  <div class="flex justify-end text-gray-600 text-sm">
    <%= l(@notice.created_at, format: '%Y-%m-%d') %>
  </div>
  <div class="text-gray-700"><%= @notice.content %></div>

  <%= link_to "戻る", notices_path, class: "text-blue-500 hover:text-blue-700 mt-4 block" %>
</div>
